# 12 — MATRIZ DE PROBLEMAS CONOCIDOS

> **Actualizado en Fase 2** (`Doc/19_PHASE2_CORE_HARDENING.md`): se añadió la columna **Estado** y se cerraron 6 de los 12 ítems de esta tabla con corrección de código real + tests escritos (no ejecutados en este entorno, ver limitación de build). Los ítems cerrados se conservan aquí, no se borran, para mantener trazabilidad (Sección 22 del prompt maestro).

| ID | Archivo | Clase/Método | Problema | Evidencia | Impacto | Severidad | Estado |
|---|---|---|---|---|---|---|---|
| BUG-01 | `MainActivity.kt`, `ui/screens/MainScreen.kt`, `ui/components/SampleRow.kt` | `DwpCreatorApp()` composable, `SampleRow.SampleContextMenu` | El menú contextual de cada fila ofrecía "Renombrar" y "Eliminar" individuales sin conectar a nada real | `[CODE]` | Sin feedback ni error al pulsar | HIGH | **MITIGADO en Fase 1**: opciones retiradas del menú (no implementadas). Implementación real sigue pendiente. |
| RISK-01 | `domain/dwp/DwpEngine.kt` | `renameRecursive()` | El reemplazo de texto operaba sobre cualquier bloque que decodificara como ASCII imprimible, sin distinguir por tag | `[CODE]` | Riesgo latente de corromper un campo binario coincidentemente imprimible | MEDIUM | **CERRADO en Fase 2**: `renameInstrument`/`renameRecursive` ahora solo tocan `RENAMABLE_TAGS` (`0x0066`, `0x0067`, `0x01f5`, `0x01f6`). Test: `renameInstrument only touches known name-path tags...` en `DwpEngineTest.kt`. |
| RISK-02 | `domain/dwp/DwpEngine.kt` | `replaceSampleAudio()` | Offsets del blob `0x01f7` verificados solo contra muestras de 32-bit float; sin evidencia para 16-bit PCM | `[CODE]` | Un export 16-bit podría escribir un blob incompleto | MEDIUM | **ABIERTO** — requiere un segundo `.dwp` de referencia real de 16-bit, no resoluble solo con código. |
| RISK-03 | `ui/components/LoadEmptyState.kt` | copy de UI | Anuncia soporte de `.dwp` suelto que `ZipProjectLoader` no implementa | `[CODE]` | Confusión de usuario | LOW | **ABIERTO** — no tocado en esta fase (fuera del alcance de "Core"). |
| RISK-04 | `domain/dwp/DwpTokenizer.kt` | `tokenize()` | Ante corrupción, un solo `break` sin distinguir causa raíz | `[CODE]` | Mensaje de error poco específico | LOW | **CERRADO en Fase 2**: nuevo `enum StopReason` (`CLEAN_EOF`/`TRAILING_BYTES`/`INVALID_LENGTH`/`PAYLOAD_TRUNCATED`/`MAX_BLOCKS_REACHED`) en `TokenizeResult`. 6 tests nuevos en `DwpEngineTest.kt`. |
| RISK-05 | `domain/dwp/DwpEngine.kt` | payloads anidados | Ninguna función verificaba `nested.endOffset == payload.size` | `[CODE]` | Riesgo de pérdida silenciosa de bytes en un `.dwp` de formato distinto | LOW | **CERRADO en Fase 1** (`tokenizeStrict`), re-verificado en Fase 2. |
| RISK-06 | `domain/io/ZipProjectLoader.kt` | `load()` | Sin límite de tamaño/cantidad de entradas al descomprimir | `[CODE]` | Riesgo de OOM ante zip malicioso | LOW-MEDIUM | **ABIERTO** — no tocado en esta fase (requiere definir límites de producto, no solo de código). |
| UX-01 | `audio/SamplePlayer.kt` | `play()` | Fallo de decode silencioso, sin feedback | `[CODE]` | UX pobre ante WAV corrupto en preview | LOW | **ABIERTO** — no tocado en esta fase. |
| INFO-01 | (raíz del repo) | Gradle Wrapper | No existe `gradlew`/`gradle-wrapper.jar` | `[CODE]` | Build local requiere Gradle preinstalado | INFO | **ABIERTO** — no bloqueante. |
| INFO-02 | `README.md`, `app/build.gradle.kts` | — | Documentación/versión desactualizada ("Paso 1 de 7") | `[CODE]` | Confunde a nuevos colaboradores | INFO | **CERRADO en Fase 2**: README actualizado (ver `11_DOCUMENTATION_AUDIT.md`). `versionName` sigue sin actualizar (cambio cosmético, no crítico). |
| BUG-02 | `domain/dwp/DwpEngine.kt`, `domain/dwp/DwpBlock.kt`, `viewmodel/DwpCreatorViewModel.kt:167` | `listSamples()` | Etiquetas `lowKey`/`rootKey` de `0x01f4` intercambiadas, con impacto funcional real: notas MIDI 0-35 no disparaban ningún sample | `[BINARY]`+`[CODE]` verificado sobre 48 muestras | Rotura funcional de selección MIDI en el rango bajo | **HIGH** (era el problema de mayor prioridad) | **CERRADO en Fase 2**: orden corregido a `[root, low, high]`. Tests: `key range physical order is root, low, high...` y `MIDI note-to-sample selection uses the corrected range...` en `DwpEngineTest.kt`. |
| P-05 | `domain/io/ZipProjectLoader.kt` | `load()` | Múltiples `.dwp` en un zip se resolvían tomando el primero en silencio | `[CODE]` | Proyecto ambiguo cargado sin aviso | HIGH | **CERRADO en Fase 2**: ahora rechaza el proyecto. Test: `rejects a zip containing more than one dwp file...`. |
| P-06 | `domain/io/ZipProjectLoader.kt` | `load()` | Colisión de nombre base de `.wav` entre carpetas distintas se resolvía sobrescribiendo en silencio dentro de un mapa | `[CODE]` | Emparejamiento de audio incorrecto sin aviso | HIGH | **CERRADO en Fase 2**: ahora rechaza el proyecto listando las rutas en conflicto. Test: `rejects a zip where two wav files in different folders share the same base name`. |
| P-07 | `domain/io/ZipProjectExporter.kt` | `export()` | Sin sanitización de `newFolderName`/`sample.name`: riesgo de path traversal (`../`) | `[CODE]` | Un `.dwp` con un nombre de sample malicioso podría escribir fuera de la carpeta esperada al exportar | HIGH | **CERRADO en Fase 2**: `requireSafeZipSegment()` rechaza `/`, `\`, `.`, `..`, vacío. Tests: `export rejects a folder name that would cause path traversal`, `export rejects a sample name containing a path separator`. |
| P-08 | `domain/io/ZipProjectExporter.kt` | `export()` | Sin detección de nombres de sample duplicados | `[CODE]` | Colisión silenciosa de entradas de zip | MEDIUM | **CERRADO en Fase 2**. Test: `export rejects two samples that ended up with the same name`. |
| P-09 | `domain/dwp/DwpEngine.kt` | `patchFrameCount()`, `replaceSampleAudio()` | Índice fuera de rango caía en no-op silencioso | `[CODE]` | El llamador no se entera de que nada se modificó | MEDIUM | **CERRADO en Fase 2**: `requireValidSampleIndex()` lanza `IllegalArgumentException` explícita. 2 tests nuevos. |
| P-10 | `midi/MidiInputManager.kt`, ahora `midi/MidiMessageParser.kt` | `parseMidiBytes()` / `MidiMessageParser.parse()` | Sin soporte de *running status*; y (encontrado en Pass 2) el running status se aplicaba incorrectamente a CUALQUIER byte de estado, incluidos mensajes de sistema (`0xF8` MIDI Clock), pudiendo corromperlo | `[CODE]` | Preview MIDI puede perder notas o interpretar mal el running status con ciertos controladores | MEDIUM | **CERRADO en Core Stabilization Pass 2**: lógica extraída a `MidiMessageParser` (Kotlin/JVM puro), running status ahora correctamente acotado a `0x80`-`0xEF`; `0xF8`-`0xFF` transparentes, `0xF0`-`0xF7` cancelan running status. 18 tests dedicados en `MidiMessageParserTest.kt` (sin ejecución real confirmada en este entorno). |
| P-11 | `audio/SamplePlayer.kt` | `play()` | `channelCount` distinto de 1/2 se trataba silenciosamente como estéreo | `[CODE]` | Audio corrupto sin aviso para formatos multicanal inusuales | MEDIUM | **CERRADO en Fase 2** (código); sin test (Android no instanciable en JVM puro). |
| P-12 | `audio/SamplePlayer.kt` | `play()` | Sin límite de voces concurrentes | `[CODE]` | Riesgo de agotar `AudioTrack` del sistema | LOW | **CERRADO en Fase 2**: cap defensivo de 24 voces (no voice-stealing). Sin test (misma limitación). |
| P-13 | `domain/dwp/DwpEngine.kt` | `patchFrameCount()`, `replaceSampleAudio()` | No validaban "exactamente un `0x01f7` de 40 bytes" antes de escribir: duplicado se parcheaba dos veces, ausente completaba sin error, tamaño incorrecto llegaba sin comprobación a `writeLE32` | `[CODE]` | Riesgo de modificación ambigua/incompleta de metadata de audio | HIGH | **CERRADO en Core Stabilization Pass 2**: `requireSingleAudioFormatBlock()` valida antes de mutar. 6 tests nuevos (ausente/duplicado/tamaño incorrecto × 2 funciones). |
| P-14 | `domain/dwp/DwpDocument.kt` | `parse()` | `PREAMBLE_SIZE=90` tratado como constante universal, sin leer el campo de versión real del archivo | `[CODE]`+`[BINARY]` (offset 4-7 confirmado = `0x26`) | Un `.dwp` de otra versión con preámbulo de tamaño distinto se parsearía incorrectamente sin aviso | MEDIUM | **CERRADO en Core Stabilization Pass 2**: nueva `DwpVersionProfile`, versión desconocida rechazada explícitamente. 4 tests nuevos. |
| P-15 | `domain/audio/WavDecoder.kt` | `decode()` | El tamaño RIFF declarado (offset 4-7) nunca se leía ni validaba; solo se usaba el tamaño físico del array | `[CODE]` | Un archivo con tamaño RIFF inconsistente se aceptaba sin aviso; un chunk podía extenderse más allá de lo que RIFF declara sin ser detectado | HIGH | **CERRADO en Core Stabilization Pass 2**: `riffSize` leído y validado; `effectiveEnd` (no `bytes.size`) es ahora el límite autoritativo. 4 tests nuevos. |
| P-16 | `domain/audio/WavDecoder.kt` | `decode()` | `WAVE_FORMAT_EXTENSIBLE` solo validaba los 2 primeros bytes del GUID, sin comprobar `cbSize` ni el resto del GUID | `[CODE]` | Un GUID no estándar que coincidiera en sus 2 primeros bytes se aceptaría incorrectamente | MEDIUM | **CERRADO en Core Stabilization Pass 2**: validación de `cbSize==22` y del sufijo GUID completo (14 bytes) contra `KSDATAFORMAT_SUBTYPE_SUFFIX`. 3 tests nuevos. |
| P-17 | `domain/io/ZipProjectLoader.kt` | `load()` | Sin límites de recursos: cantidad de entradas, tamaño de entrada individual, tamaño total descomprimido | `[CODE]` | Riesgo de OOM ante un zip malicioso ("zip bomb") | MEDIUM-HIGH | **CERRADO en Core Stabilization Pass 2**: `MAX_ENTRIES`/`MAX_SINGLE_ENTRY_BYTES`/`MAX_TOTAL_UNCOMPRESSED_BYTES`, verificados **durante** la descompresión. 4 tests nuevos. |
