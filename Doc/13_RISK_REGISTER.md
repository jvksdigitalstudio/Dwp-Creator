# 13 — REGISTRO DE RIESGOS

> **Actualizado en Fase 2** (`Doc/19_PHASE2_CORE_HARDENING.md`): R-01, R-04, R-06 y R-09 se cierran con corrección de código + tests escritos. Se conservan aquí con su historial completo (Sección 22 del prompt maestro no permite borrar información útil).

| ID | Riesgo | Probabilidad | Impacto | Severidad | Evidencia | Mitigación aplicada / propuesta | Estado |
|---|---|---|---|---|---|---|---|
| R-01 | Renombrado global corrompe un campo binario que casualmente decodifica como texto imprimible coincidente con el nombre viejo | Baja con archivos como el de referencia; desconocida para otros `.dwp` | Alto si ocurre | MEDIUM | `[CODE]`+`[BINARY]` | **APLICADA en Fase 2**: `renameInstrument` restringido a `RENAMABLE_TAGS` (allow-list de 4 tags conocidos) | **CERRADO** |
| R-02 | Offsets de `0x01f7` incorrectos para audio 16-bit PCM en un export real | Desconocida (sin fixture de referencia 16-bit) | Alto | MEDIUM | `[CODE]`: riesgo auto-documentado | Conseguir/crear un `.dwp` de referencia 16-bit real | **ABIERTO** — no resoluble solo con código |
| R-03 | Tag `0x0206` (audio embebido FLAC) hipotético sin evidencia real | Alta si se implementa sin evidencia | Alto | HIGH | `[UNKNOWN]` total | No implementar sin `.dwp` monolítico real de referencia | **ABIERTO** — bloqueante para Fase 6/7 del plan objetivo, explícitamente fuera de alcance de esta fase |
| R-04 | OOM por zip/`.dwp` de tamaño extremo o malformado | Baja en uso normal, alta ante archivos no confiables | Medio | LOW-MEDIUM | `[CODE]` | **APLICADA COMPLETA en Core Stabilization Pass 2**: overflow de aritmética corregido en Fase 2 (`DwpTokenizer`/`WavDecoder`); límites explícitos `MAX_ENTRIES`/`MAX_SINGLE_ENTRY_BYTES`/`MAX_TOTAL_UNCOMPRESSED_BYTES` añadidos en `ZipProjectLoader`, verificados **durante** la descompresión (defensa real contra zip bomb, no solo un chequeo posterior) | **CERRADO** |
| R-05 | Funcionalidad de UI "fantasma" (renombrar/eliminar sample individual) | Alta | Bajo-Medio | HIGH (visibilidad) | `[CODE]`: BUG-01 | Ya mitigado en Fase 1 (opciones retiradas) | **MITIGADO** (Fase 1), implementación real sigue pendiente |
| R-06 | Pérdida de bytes en payload anidado de sample container en un `.dwp` de formato distinto | Desconocida — 0% observado en el fixture disponible | Alto si ocurre | LOW | `[CODE]`+`[BINARY]` | Ya cerrado en Fase 1 (`tokenizeStrict`) | **CERRADO** (Fase 1, re-verificado en Fase 2) |
| R-07 | Ausencia de Gradle Wrapper | Cierta | Bajo | INFO | `[CODE]` | Generar wrapper | **ABIERTO**, no bloqueante |
| R-08 | Sesgo de fixture único: `Instrument.dwp` no tiene variación interna en los 10 campos de `0x01f7` | Cierta | Alto para investigación DWP | HIGH (investigación) | `[BINARY]` | Conseguir un segundo `.dwp` de referencia real con loop/bit-depth/canales distintos | **ABIERTO** — bloqueante para investigación, no para el código; explícitamente la "próxima fase" de este prompt |
| R-09 | `BUG-02`: etiquetas `lowKey`/`rootKey` intercambiadas, con impacto funcional real confirmado en la selección MIDI (`DwpCreatorViewModel.kt:167`) | Era cierta en el código anterior | Alto (preview MIDI roto en el rango bajo) | HIGH | `[BINARY]`+`[CODE]` | **APLICADA en Fase 2**: orden corregido a `[root, low, high]` en `DwpEngine.listSamples()`; KDoc de `DwpBlock.kt` corregido; 2 tests nuevos verifican tanto los valores como el comportamiento de selección MIDI real | **CERRADO** |
| R-10 | `ZipProjectLoader` aceptaba múltiples `.dwp` (tomando el primero) y sobrescribía en silencio colisiones de nombre base de `.wav` entre carpetas | Cierta en el código anterior | Alto (proyecto ambiguo cargado sin aviso, o audio mal emparejado) | HIGH | `[CODE]` | **APLICADA en Fase 2**: ambos casos ahora rechazan el proyecto explícitamente. 2 tests nuevos | **CERRADO** |
| R-11 | `ZipProjectExporter` sin sanitización de nombres: riesgo de path traversal vía un `sample.name` proveniente de un `.dwp` malicioso/corrupto | Baja en uso normal, cierta si se expone a `.dwp` no confiables | Alto (escritura fuera de la carpeta esperada al extraer el zip) | HIGH | `[CODE]` | **APLICADA en Fase 2**: `requireSafeZipSegment()` rechaza `/`, `\`, `.`, `..`, nombres vacíos y duplicados. 3 tests nuevos | **CERRADO** |
| R-12 | `DwpEngine.patchFrameCount`/`replaceSampleAudio` con índice fuera de rango caían en no-op silencioso | Cierta en el código anterior | Medio (el llamador no se entera de que nada cambió) | MEDIUM | `[CODE]` | **APLICADA en Fase 2**: `requireValidSampleIndex()` lanza excepción explícita. 2 tests nuevos | **CERRADO** |
| R-13 | `MidiInputManager` sin soporte de running status; y (encontrado en Core Stabilization Pass 2) el running status se aplicaba incorrectamente a mensajes de sistema (`0xF8` MIDI Clock), pudiendo corromperlo | Cierta en el código anterior | Medio (preview MIDI incompleto o running status corrompido) | MEDIUM | `[CODE]` | **APLICADA COMPLETA en Core Stabilization Pass 2**: lógica extraída a `MidiMessageParser` (Kotlin/JVM puro), running status correctamente acotado a mensajes de canal (`0x80`-`0xEF`); System Realtime/Common manejados por separado. 18 tests dedicados en `MidiMessageParserTest.kt` | **CERRADO** (tests escritos, no ejecutados en este entorno) |
| R-14 | `SamplePlayer` trataba cualquier `channelCount` ≠1 como estéreo; sin límite de voces concurrentes | Cierta en el código anterior | Medio (audio corrupto silencioso / agotamiento de recursos) | MEDIUM/LOW | `[CODE]` | **APLICADA en Fase 2**: validación de canal + cap de 24 voces | **CERRADO** (código); sin test automatizado (misma limitación que R-13) |
